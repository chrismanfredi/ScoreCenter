'use strict';

/**
 * @ngdoc function
 * @name scoreCenterApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the scoreCenterApp
 */
// app.controller('GameComments', ['$scope', '$firebase', function($scope, $firebase) {
// 	var ref = new Firebase("https://scorecenter.firebaseio.com");
// 	$scope.comments = $firebase(ref);
// 	$scope.username = 'Guest' + Math.floor(Math.random() * 101);

// 	$scope.addComment = function(e) {
// 		if (e.KeyCode != 13) return;
// 		$scope.comments.$add({
// 			from: $scope.username,
// 			body: $scope.newComment
// 		});
// 		$scope.newComment = "";
// 		}
// 	}
// ]);
